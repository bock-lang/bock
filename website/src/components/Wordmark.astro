---
interface Props {
  size?: number;
}

const { size = 28 } = Astro.props;
const style = `font-family:var(--font-wordmark);font-weight:600;font-size:${size}px;letter-spacing:-0.02em;color:var(--fg);line-height:1;`;
---

<span style={style}>bock</span>
