# §13.5 cancellation: trim to wired surface, defer remainder to v1.x

**Date:** 2026-05-14
**Affects:** §13.5
**Type:** clarification + deferral
**Part of:** Pending items from D1+D2 spec alignment consolidation

## Summary

§13.5 currently describes a substantial cancellation surface: ambient `Cancel` effect, automatic checkpoint insertion (await, effect ops, loop iterations), explicit `check_cancel()` builtin, `task.cancel()` API, structured concurrency propagation, target mapping to native cancellation primitives, and strictness gating. The implementation wires only the adaptive handler integration: cancellation flag on combinators, `on_cancel` hook on `RecoveryStrategy`, cancellation-aware await points within built-in combinators. The rest is not implemented.

This changelog restructures §13.5 into two subsections:

- **§13.5.1 — Cancellation in adaptive handlers (v1):** the wired surface, kept normative and slightly expanded for self-contained reading
- **§13.5.2 — Full cancellation surface (Reserved for v1.x):** the broader cancellation model preserved as design intent with explicit Reserved marker

The restructure trims §13.5's normative claims to what the implementation actually ships while preserving the design sketch for v1.x. The wholesale §13.5 surface is not removed; it is gated behind a Reserved marker until v1.x adds the missing pieces.

## Change

Replace the current §13.5 body with the restructured two-subsection form below.

### §13.5 — Cancellation (intro)

> Cancellation is modeled as an ambient effect (`Cancel`) available in async contexts. v1 supports cancellation through the adaptive handler integration described in §13.5.1, which exercises the `Cancel` effect at recovery-strategy boundaries. The full cancellation surface described in §13.5.2 (cooperative checkpoints throughout user code, explicit `check_cancel()`, `task.cancel()` API, target mapping, strictness gating) is Reserved for v1.x.

### §13.5.1 — Cancellation in adaptive handlers (v1)

> Adaptive handlers per §10.8 integrate with cancellation:
>
> - The `RecoveryStrategy` trait's `attempt` method returns `Result[T, E] | Cancelled`, allowing strategies to surface cancellation explicitly
> - The `on_cancel(self, context: RecoveryContext) -> Void` hook (default no-op) fires when the enclosing task is cancelled while a strategy is executing, enabling cleanup of external state
> - Built-in combinators (`retry`, `circuit_break`, `use_cached`, `degrade`, `escalate`) check cancellation at their internal await points
> - A strategy returning `Cancelled` halts the adaptive handler immediately; no further strategies are attempted
> - The adaptive handler propagates `Cancelled` to its caller through the same channel as ordinary errors
>
> v1 user code observes cancellation in two ways:
>
> 1. By implementing a custom `RecoveryStrategy` that handles `Cancelled` in its `attempt` return
> 2. By being called from within an adaptive handler whose enclosing task is cancelled (cancellation propagates through the handler's `Cancelled` return)
>
> See §10.8 for the full adaptive handler specification including `RecoveryContext`, the built-in combinator semantics, and the manifest treatment of cancellation events.

### §13.5.2 — Full cancellation surface (Reserved for v1.x)

> The broader cancellation model described in this subsection is **Reserved for v1.x**. v1 compilers reject the constructs described below at the relevant pipeline stage (parse, type-check, or codegen) with a "Reserved for v1.x" diagnostic. v1 users who need cancellation observe it through the adaptive handler integration in §13.5.1; the constructs below are the v1.x extension that makes cancellation available throughout user code.
>
> #### Cooperative checkpoints
>
> The compiler inserts cancellation checks at well-defined points:
>
> - Every `await` expression
> - Every effect operation invocation (`with Clock`, `with Network`, etc.)
> - Explicit `check_cancel()` calls for tight loops that don't otherwise reach a checkpoint
> - Loop iteration boundaries in `@concurrent` blocks
>
> At each checkpoint, if cancellation has been signaled, the task propagates a `Cancelled` value through the call stack. This is not an exception; it is a typed return value tracked by the type system like any other `Result`-like outcome.
>
> #### Requesting cancellation
>
> A task handle exposes `cancel()`:
>
> ```bock
> let task = @concurrent { long_running_operation() }
> // ... later
> task.cancel()
> let result = await task   // returns Cancelled
> ```
>
> Structured concurrency: cancelling a task cancels all of its child tasks transitively. `@concurrent { ... }` blocks propagate cancellation to every operation started within them.
>
> #### Checking cancellation manually
>
> ```bock
> fn compute_intensive(data: List[Int]) -> Result[Summary, Cancelled] with Cancel {
>   let mut acc = 0
>   for (i, x) in data.enumerate() {
>     if (i % 1000 == 0) { check_cancel()? }
>     acc = acc + expensive(x)
>   }
>   Ok(summarize(acc))
> }
> ```
>
> The `?` propagates `Cancelled` the same way it propagates `Err`. Functions that observe cancellation declare the `Cancel` effect; functions that only pass through cancellation do not need to declare it (the ambient effect is always available in async contexts).
>
> #### Target mapping
>
> The transpiler maps the `Cancel` effect to each target's native mechanism:
>
> | Target | Mechanism                          |
> |--------|------------------------------------|
> | Rust   | `tokio::sync::CancellationToken`   |
> | JS/TS  | `AbortSignal`                      |
> | Go     | `context.Context` with `Done()`    |
> | Python | `asyncio.Task.cancel()` + check    |
>
> #### Cancellation and cleanup
>
> Code that holds resources across a checkpoint must handle cancellation explicitly. The `with` handler mechanism provides the standard cleanup pattern; handlers can register cleanup on the `Cancel` effect to release resources when the enclosing task is cancelled.
>
> #### Strictness interaction
>
> | Level | Cancellation behavior |
> |---|---|
> | `sketch` | Checkpoints auto-inserted; no annotations required |
> | `development` | Long-running operations (loops, recursion) warned if no `check_cancel()` reachable |
> | `production` | Error if a `@concurrent` or `async` function has no reachable checkpoint within a configurable depth bound |
>
> #### v1.x design pass scope
>
> v1.x cancellation has three implementation layers, each of which warrants its own design pass within the v1.x roadmap:
>
> 1. **Builtin and API surface:** `check_cancel()` prelude function, `task.cancel()` method on task handles, `Cancelled` type, `Cancel` effect availability in non-async contexts (if any).
> 2. **Compiler-inserted checkpoints:** AIR lowering pass that inserts cancellation checks at `await` expressions, effect operations, and loop boundaries. Interaction with the existing AIR optimization passes (if any) and effect erasure (§10.6).
> 3. **Target mapping codegen:** per-target codegen that maps the `Cancel` effect to the table above. Each target has its own native cancellation primitive with its own propagation semantics; the codegen pass produces equivalent observable behavior per the §20.4 cross-target correctness principle.
> 4. **Strictness gating:** static analysis to identify long-running operations without reachable checkpoints; warning and error diagnostics per the strictness table above.
>
> These layers may ship across v1.x point releases rather than all at once. The v1.x roadmap entry will decide whether to ship them as a bundle or incrementally.

## Rationale

### Why trim rather than defer wholesale

The adaptive handler cancellation integration works today. It is exercised by `RecoveryStrategy` implementations and is part of the §10.8 adaptive handler design. Deferring §13.5 entirely would lose a working integration in the spec; that loss would create confusion for users reading §10.8's cancellation references that point to a deferred section.

Keeping §13.5.1 normative documents the v1 surface accurately. Keeping §13.5.2 as Reserved with explicit design sketch preserves v1.x design intent and gives the v1.x roadmap a clear entry point.

### Why preserve the v1.x design sketch in spec

The §13.5 surface as originally written is detailed: ambient effect, checkpoint locations, target mapping table, strictness behavior. That level of detail is hard to reconstruct from scratch. Preserving the sketch with a Reserved marker lets v1.x design work pick up where v1 left off rather than starting from zero.

This matches the pattern used in other deferral changelogs (FFI's §14.1, AIR serialization's §16.3): defer the implementation but preserve the design content.

### Why the four-layer breakdown

v1.x cancellation is not a single design pass; it has distinct layers (API surface, checkpoint insertion, target mapping, strictness gating). Each layer has its own design and implementation concerns:

- **API surface** is small-scope language design (prelude function + task handle method)
- **Checkpoint insertion** is compiler-internal (AIR pass; interacts with optimization passes)
- **Target mapping codegen** is target-specific (one codegen pass per shipping target; interacts with each target's async runtime)
- **Strictness gating** is static analysis (control-flow analysis to find unreachable checkpoints)

Naming the layers explicitly in the Reserved subsection gives v1.x clear sub-projects rather than treating cancellation as one monolithic v1.x deliverable.

### Why this is impl-defined (not impl-must-update)

The current implementation matches the trimmed §13.5.1 surface. No code changes are required. The spec changes align prose with existing impl state.

The Reserved-marker rejection (parse/type-check/codegen rejecting `check_cancel()`, `task.cancel()`, etc.) may already happen by default (unknown identifier errors) or may need explicit diagnostics for better user experience. That's a polish question, not a v1 blocker. If the impl chat wants to add explicit "Reserved for v1.x" diagnostics for the constructs in §13.5.2, that can land as a follow-up impl task.

## Implementation Notes

Category: **impl-defined** for the core restructure; **future-impl** for the v1.x layers.

**v1 impl status:**

- Adaptive handler cancellation integration (§13.5.1): wired and working today per `bock-core/src/adaptive.rs:286,584–608`
- `check_cancel()` builtin (§13.5.2): not present
- `task.cancel()` API (§13.5.2): not present
- Compiler checkpoint insertion (§13.5.2): not present
- Target mapping codegen (§13.5.2): not present
- Strictness gating (§13.5.2): not present

The trimmed §13.5.1 matches what the impl ships. No code changes required for the spec restructure.

**Optional polish work:**

If the impl chat wants to add explicit "Reserved for v1.x" diagnostics when users write `check_cancel()` or `task.cancel()` in v1 code, the work is:

- Recognize `check_cancel` and `task.cancel` as Reserved identifiers
- Emit "Reserved for v1.x" diagnostic instead of the generic unknown-identifier error
- Suggest alternative approaches in the diagnostic message (e.g., adaptive handler for v1)

This is polish, not blocking. The current generic-error behavior is acceptable in the interim.

**v1.x roadmap entries:**

The v1.x roadmap should carry entries for each of the four cancellation layers:

- API surface (small scope; could ship in v1.x.0)
- Checkpoint insertion (compiler-internal; mid-size)
- Target mapping codegen (per-target work; spread across point releases)
- Strictness gating (static analysis; mid-size)

These are tracked in ROADMAP.md; this changelog does not commit to specific timing.

## Migration

For users (none yet, since v1 hasn't shipped): user code that observes cancellation today does so through adaptive handlers. The restructure does not change that surface; it makes the v1 vs v1.x boundary explicit.

For the spec: §13.5 grows from a single section to two subsections. The content of §13.5.2 is preserved (with one new subsection on v1.x design pass scope) but is now gated behind a Reserved marker. Cross-references from §10.8 to §13.5 continue to work; they now point at §13.5.1 specifically.

## Cross-References

- §10.8 adaptive handlers (the wired cancellation integration lives here; §13.5.1 summarizes and cross-references)
- §10.6 effect erasure (Reserved for v1.x per the prior changelog; interacts with v1.x cancellation checkpoint insertion since erased effects don't reach checkpoints)
- §13.3 channels (paired surface; bounded channels Reserved for v1.x per the D1+D2 batch)
- §13.4 sync primitives (Reserved for v1.x per the D1+D2 batch)
- §17.4 strictness levels (the strictness gating in §13.5.2 references the same `sketch`/`development`/`production` model)
- §20.4 cross-target correctness principle (paired changelog `20260514-0408-specs-changes.md`; cancellation codegen must produce equivalent observable behavior across targets when v1.x ships target mapping)
- §15 annotation taxonomy (paired changelog `20260514-0408-specs-changes.md`; the `@concurrent` annotation in §13.5.2's strictness table is one of the codegen-consumed annotations)

## Closing Note

This is the third and final pending design item from the D1+D2 spec alignment consolidation. The pending queue is now clear:

- §15 annotation taxonomy: resolved by changelog `20260514-0408-specs-changes.md` (recognition policy, category restructure, runtime guardrails Reserved, @benchmark removed, §20.4 architectural principle added)
- §18.5 trait integration conformance: resolved by changelog `20260514-0412-specs-changes.md` (enumeration expanded, conformance test surface added, paired impl session scheduled)
- §13.5 cancellation restructure: resolved by this changelog (§13.5.1 v1 normative, §13.5.2 Reserved for v1.x with four-layer design pass scope)

With these three changelogs, the D1+D2 alignment work has a complete spec-side resolution. Implementation chat picks up the paired impl tasks (§18.5 conformance suite, optional §13.5 Reserved-identifier diagnostics, the broader D1+D2 batch from `20260513-*` changelogs). No further design-chat items are queued.

Future design work continues on the two architectural directions surfaced earlier (AI layer composability as v1.x; internal codegen adapter refactor as v1.x), but those are forward design, not pending consolidation items.
