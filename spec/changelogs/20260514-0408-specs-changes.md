# §15 annotation taxonomy: recognition policy, category restructure, runtime guardrails reserved

**Date:** 2026-05-14
**Affects:** §15 (restructure), §20.4 (architectural principle added)
**Type:** clarification + addition + deferral
**Part of:** Pending items from D1+D2 spec alignment consolidation

## Summary

§15 is restructured from a flat enumeration of ~20 annotations into a category-based taxonomy with explicit recognition policy, routing per annotation, and a named Reserved subsection for runtime guardrails. The changes:

1. **Recognition policy** stated explicitly: unknown annotations are an error in all strictness modes.
2. **Categories** introduced: codegen-consumed, test-runner-consumed, semantic context (compile-time), Reserved for v1.x.
3. **@benchmark removed entirely** from the spec; performance benchmarking is delegated to target-native tools.
4. **Runtime guardrails** named as Reserved for v1.x with the verb-keyed payload pattern (`@performance(track: ...)`, `@invariant(assert: ...)`, `@security(audit: ...)`, `@domain(enforce: ...)`).
5. **Module-level annotations** Reserved for v1.x; per-declaration application is the v1 form.
6. **§20.4** gains an architectural principle paragraph: cross-target correctness is owned by Bock; performance is delegated to target-native tools.

The restructure is substantial. §15 today is a flat list that doesn't scale to the actual diversity of annotation behaviors. The new structure handles routing, scope, and reservation explicitly.

## Change

### §15.0 (new) Recognition policy

Add a new opening subsection to §15:

> ### §15.0 Recognition policy
>
> Unknown annotations are a compile-time error in all strictness modes. A v1 compiler that encounters `@foo` without knowing what `@foo` means rejects the source with an "unknown annotation" diagnostic.
>
> This policy applies uniformly: there is no "silent" or "warn" tier. Bock is feature-declarative: annotations encode semantic intent (capabilities, security boundaries, performance hints). Silent failure on typos like `@invarient` or `@requirs(Auth)` would be dangerous, and a warn tier creates the question of "which strictness am I in?" that the uniform-error policy eliminates.
>
> Annotations are "known" when they appear in:
>
> - The §15 taxonomy below (built-in annotations)
> - A registered plugin's annotation surface (per Appendix C plugin system, Reserved for v1.x)
>
> v1 has no plugin system; the built-in taxonomy is the complete known set. v1.x extends "known" to include plugin-declared annotations.

### §15.1 (new) Codegen-consumed annotations

Reorganize the 13 currently wired annotations under a Codegen-consumed heading. These flow through the C-AIR context interpreter and influence codegen and AI provider Generate mode behavior:

> ### §15.1 Codegen-consumed annotations
>
> These annotations are consumed by the C-AIR context interpreter and inform codegen decisions and AI provider Generate mode context.
>
> | Annotation | Purpose |
> |------------|---------|
> | `@context` | Declares contextual scope for code blocks; flows to AI provider |
> | `@requires(...)` | Declares required capability; verified at compile time |
> | `@performance` | Marks performance-critical code; influences synthesis (see §15.4 for runtime guardrail v1.x extension) |
> | `@invariant` | Declarative state invariant (see §15.4 for runtime guardrail v1.x extension) |
> | `@security` | Security-relevant marker; interacts with §11.8 PII propagation (see §15.4 for runtime guardrail v1.x extension) |
> | `@domain` | Domain boundary marker (see §15.4 for runtime guardrail v1.x extension) |
> | `@concurrent` | Marks code as concurrent-safe |
> | `@managed` | Marks code as memory-managed (vs. ownership-tracked) |
> | `@deterministic` | Declares pure determinism |
> | `@inline` | Codegen inlining hint |
> | `@cold` | Hot-path optimization hint (cold = rarely executed) |
> | `@hot` | Hot-path optimization hint (hot = frequently executed) |
> | `@deprecated` | Deprecation marker; produces compile-time diagnostic on use |

### §15.2 (new) Test-runner-consumed annotations

Two annotations are consumed by the test runner rather than codegen:

> ### §15.2 Test-runner-consumed annotations
>
> These annotations are consumed by `bock test`, not by the C-AIR context interpreter. The test runner discovers test functions by annotation and routes them to the configured test framework per target.
>
> | Annotation | Purpose |
> |------------|---------|
> | `@test` | Marks a function as a test; included in `bock test` runs |
> | `@test(skip)` | Marks a test as skipped without removing it |
>
> The test runner reads these annotations directly from source; they do not flow through C-AIR codegen paths. Test functions are excluded from production builds (`bock build --no-tests` per §20.1).

### §15.3 (new) Application sites

Document where annotations may appear:

> ### §15.3 Application sites
>
> In v1, annotations apply to individual declarations: `fn`, `record`, `effect`, `module` members. Each annotation attaches to the declaration immediately following it.
>
> **Module-level annotations on the `module` declaration itself are Reserved for v1.x.** The form `@context @requires(Auth) module accounts.api { ... }` (applying annotations across every declaration in a module) is planned for v1.x as a syntactic convenience. v1 users who want module-wide annotation semantics annotate each declaration individually.

### §15.4 (new) Reserved for v1.x

Consolidate Reserved annotations and the runtime guardrail family:

> ### §15.4 Reserved for v1.x
>
> The following annotation surfaces are Reserved for v1.x. The reserved syntax is rejected by v1 compilers per §15.0; v1.x adds the routing.
>
> #### Annotation deferrals
>
> | Annotation | Routing | Status |
> |------------|---------|--------|
> | `@property` | Property-based testing framework (extends `@test` infrastructure) | Reserved for v1.x pending stdlib property-based testing |
> | `@derive` | Codegen extension (generates impls) | Reserved for v1.x via plugin system (Appendix C) |
> | `@target` | Conditional compilation by codegen target | Reserved for v1.x with FFI (§14.1) |
> | `@platform` | Conditional compilation by platform | Reserved for v1.x with FFI (§14.1) |
>
> #### Runtime guardrails
>
> The semantic context annotations (`@performance`, `@invariant`, `@security`, `@domain`) are compile-time context in v1, used to inform codegen and AI provider Generate mode. v1.x is reserved for runtime guardrail variants of each, distinguished by a verb-keyed payload:
>
> | Annotation form | Verb | Planned runtime semantics |
> |-----------------|------|--------------------------|
> | `@performance(track: ...)` | track | Runtime timing instrumentation with threshold-based reporting |
> | `@invariant(assert: ...)` | assert | Runtime assertion checking; violation fails per configured severity |
> | `@security(audit: ...)` | audit | Runtime audit event emission on access |
> | `@domain(enforce: ...)` | enforce | Runtime domain boundary enforcement at call sites |
>
> The runtime guardrails are not "deferred" in the sense of a half-built feature; they are a named future direction whose design pass happens when v1 stabilizes and concrete use cases inform the verb semantics. v1 compilers reject the verb-keyed payload forms as unknown annotation variants per §15.0.
>
> #### Removed entirely
>
> `@benchmark` was previously enumerated in §15. It is removed in v1 and not Reserved. Performance benchmarking is delegated to target-native tools (see §20.4); a Bock-level benchmark annotation does not fit the architecture.

### §20.4 architectural principle (new paragraph)

Add an architectural principle paragraph to §20.4 (Testing Tiers) following the existing tier definitions:

> #### Cross-target correctness vs. performance
>
> Bock owns cross-target correctness verification: a `@test` function that passes on JavaScript must also pass on Python, Rust, and every other target the codebase ships to. This is the architectural value of the cross-target testing tier: it operationalizes the cross-target semantic equivalence claim that distinguishes Bock from a target-by-target source-to-source transpiler.
>
> Performance, by contrast, is delegated to target-native tools. Bock does not own benchmarking. Performance varies wildly across targets by design (target choice is itself a performance decision), and every target ships mature benchmark tooling (`cargo bench`, `pytest-benchmark`, `npm run bench`, `go test -bench`). Users who care about performance benchmark the transpiled output with the target's native tools directly. This is why §15 does not include a `@benchmark` annotation; the cross-target value proposition of a unifying benchmark surface does not exist.
>
> Runtime guardrail variants of the semantic context annotations (§15.4) are a separate concern: they verify constraints at runtime (assertions, audit logging, performance thresholds), not benchmark performance for comparison. The guardrails express user-declared invariants that should hold; they do not measure aggregate performance characteristics.

## Rationale

### Recognition policy

Silent unknown annotations are dangerous for a feature-declarative language. The original spec did not specify behavior; the impl chose silent. Codifying error-everywhere makes typos catchable and removes the question of "what strictness am I in?" from the user's mind.

The plugin system (Appendix C, Reserved) becomes the path for user-defined annotations. v1 has the built-in taxonomy; v1.x extends "known" to plugin-declared annotations. Until plugins ship, the v1 taxonomy is the complete set, which is a useful constraint.

### Category restructure

The flat list in current §15 doesn't capture meaningful differences in annotation behavior. The four routing categories (codegen-consumed, test-runner-consumed, semantic-context-with-runtime-guardrails, Reserved) reflect what the impl actually does and what the design intends. Documenting them explicitly:

- Surfaces the routing for each annotation
- Makes Reserved annotations clearly findable
- Names the runtime guardrail family as a v1.x design entry point
- Removes the silent-drop behavior for the 6 previously-unrouted annotations (they're now either categorized or Reserved)

### @benchmark removal

The benchmark question surfaced during the design discussion: what does benchmarking a Bock program even mean? Three possible meanings (against the Bock interpreter, across transpiled targets, or not meaningful at the Bock level). The third is correct.

Performance is per-target by design. Cross-target benchmark equivalence is not desirable (different targets have different performance characteristics; that's the whole point of choosing a target). A `bock bench` command would have to delegate to target-native tools, which is just running those tools directly. The annotation adds no value.

Removing @benchmark (rather than deferring it) is the honest disposition. The architectural principle in §20.4 makes the boundary explicit so the design rationale is recorded.

### Runtime guardrails Reserved

The discussion that produced this changelog surfaced an implicit question: do the semantic context annotations have runtime implications? If `@performance` requires runtime timing infrastructure, Bock is committing to per-target performance packages across all targets. That conflicts with the "performance delegated to targets" principle.

The resolution: compile-time only in v1; runtime variants Reserved with explicit verb-keyed payload syntax. The verb-keyed convention (`track`, `assert`, `audit`, `enforce`) reserves the syntactic space, gives v1.x a labeled entry point, and prevents conflicting future designs for the same payload form.

This is structurally different from "deferring" a feature. The runtime guardrails are a named future direction with reserved syntax, not a half-built capability waiting for completion.

### Module-level annotations

Per-declaration annotations cover the use case. Module-level is convenience. Deferring to v1.x is straightforward; the parsing work is bounded but not v1-critical.

## Implementation Notes

Category: **impl-must-update**.

**Required impl updates for v1:**

1. **§15.0 recognition policy**: replace the catch-all `_ => {}` arm in `compiler/crates/bock-air/src/context.rs:195–211` with an explicit "unknown annotation" diagnostic. All annotation tokens not matching the §15.1, §15.2, or §15.3 forms become compile-time errors.

2. **@benchmark removal**: the spec change removes `@benchmark` from the enumerated set; impl must reject `@benchmark` as unknown per §15.0. Existing test fixtures or example code using `@benchmark` should be migrated to `@test` or removed.

3. **@test routing surface**: confirm that the test runner discovers test functions via `@test` annotation rather than (or in addition to) naming convention. If discovery is convention-based today, add explicit annotation routing.

4. **Reserved syntax rejection**: v1 compilers must reject the verb-keyed payload forms (`@performance(track: ...)`, etc.) as unknown annotation variants. The reserved syntax space is "known to be Reserved" at the spec level but "unknown" at the v1 compiler level until v1.x adds routing.

**Paired session scope:** Single bounded session for the impl updates. Acceptance criteria:

- Unknown annotation produces compile-time error with helpful diagnostic
- @benchmark rejected; no remaining references in fixtures
- @test routing wired through test runner (may already be in place; verify)
- Verb-keyed payload forms rejected with "Reserved for v1.x" diagnostic
- Module-level annotation form on `module` declaration rejected with "Reserved for v1.x" diagnostic

If the impl session uncovers complexity beyond bounded estimate (for example, the test runner currently relies on a different discovery mechanism that doesn't trivially route through @test), surface as OPEN and we revisit specific parts.

## Migration

For users (none yet, since v1 hasn't shipped, but worth documenting for the changelog record):

- Code using `@benchmark` should switch to target-native benchmark tools
- Code using `@invarient` or similar typos previously passed silently; will now error
- Code attempting module-level annotation forms must annotate each declaration individually

For the impl:

- The `_ => {}` catch-all is replaced with an error diagnostic
- @benchmark is removed from any enumerated set in code
- Verb-keyed payload forms get a "Reserved for v1.x" rejection path

## Cross-References

- §11.8 PII propagation (interacts with `@security` compile-time scope)
- §17.4 strictness modes (recognition policy is uniform across modes, not strictness-tier-dependent)
- §17.6 capability gap synthesis (Generate mode receives semantic context annotations as context)
- §20.4 Testing Tiers (paired architectural principle added by this changelog)
- Appendix C Plugin System (path for user-defined annotations in v1.x)
- §14.1 FFI / native blocks (Reserved with `@target` and `@platform`)
- §13.5 cancellation (next pending design item; unrelated to this changelog)
- §18.5 trait integration conformance (next pending design item; unrelated to this changelog)

## Closing Note

This is the first of three pending design items from the D1+D2 spec alignment consolidation. §13.5 cancellation restructure and §18.5 trait integration conformance remain to be resolved. The §15 work surfaced two architectural principles worth flagging: the "performance delegated to targets" principle (now in §20.4) and the "compile-time vs runtime annotation scope" distinction (now in §15.4). Both are foundational decisions whose downstream implications may surface in future design passes.
