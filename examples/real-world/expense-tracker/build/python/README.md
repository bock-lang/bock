# expense-tracker

Python output generated by [Bock](https://bock-lang.org) (project mode).

## Run

```sh
python3 main.py
```

## Test

Tests are generated as pytest tests:

```sh
pip install -e .
pytest
```

> Generated by `bock build`. Re-running the build regenerates this output.
